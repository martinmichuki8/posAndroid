<?xml version="1.0" encoding="utf-8"?>
<shortcut xmlns:android="http://schemas.android.com/apk/res/android">
    <shortcut
        android:shortcutId="compose"
        android:enabled="true"
        android:icon="@drawable/add"
        android:shortcutShortLabel="@string/Add_Products">
        <intent
            android:action="android.intent.action.VIEW"
            android:targetPackage="com.michtech.pointofSale"
            android:targetClass="com.michtech.pointofSale.Ui.more.store.AddProducts" />
        <!-- If your shortcut is associated with multiple intents, include them
             here. The last intent in the list determines what the user sees when
             they launch this shortcut. -->
        <categories android:name="android.shortcut.conversation" />
    </shortcut>

</shortcut>